import { User } from "../entities/seller/Seller.js"

export class UserService {
    new(input: {
        username: string
        email: string
        password: string
        role: string
    }) {
	User.
    }
}
